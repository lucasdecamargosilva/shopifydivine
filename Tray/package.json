{
  "name": "provador-tray",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "build": "cd painel && npm run build"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
